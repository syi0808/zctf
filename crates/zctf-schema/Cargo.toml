[package]
name = "zctf-schema"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Schema IR and canonical artifacts for zctf"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
